import { Buffer } from 'buffer';

export interface Student {
    _id: string;
    nombre: string;
    aPaterno: string;
    aMaterno: string;
    matricula: string;
    carrera: string;
    correo: string;
    password: string;
}
